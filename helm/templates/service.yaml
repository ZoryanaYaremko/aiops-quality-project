apiVersion: v1
kind: Service
metadata:
  name: aiops-quality-project
  labels:
    app: aiops-quality-project
spec:
  type: {{ .Values.service.type }}
  selector:
    app: aiops-quality-project
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
